profiles:
  original:
    entry: experiments/kitti/rq1_scripts_original/calculate_rq1_table_from_csv.py
    notes: lightweight table generation from existing csv/tex artifacts

  paper_kitti_main:
    entry: experiments/kitti/pipeline/tools_py/run_kitti_eval.py
    args_template:
      kitti_root: /path/to/KITTI
      sources: combo,objects

  paper_kitti_gpu:
    entry: experiments/kitti/pipeline/tools_py/run_kitti_eval_gpu.py
    args_template:
      kitti_root: /path/to/KITTI
      sources: combo,objects
