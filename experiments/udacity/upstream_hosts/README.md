# Udacity Upstream Hosts

This directory stores selected upstream scripts copied from remote mirrors for RQ2 integration.

- `host172`: primary run orchestration and offline evaluator stack.
- `host114`: backup variant for comparison and provenance.

Use `scripts/run/run_rq2_udacity.sh --profile ...` to choose the active host profile.
