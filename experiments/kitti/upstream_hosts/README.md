# KITTI Upstream Hosts

This directory stores selected upstream scripts copied from remote mirrors for RQ1 integration.

- `host172/tools_py`: KITTI orchestration and metric scripts.
- `host172/did_m3d_tools`: detector-side evaluation helper.

These files are kept for provenance and should be wrapped by adapters rather than edited in place.
