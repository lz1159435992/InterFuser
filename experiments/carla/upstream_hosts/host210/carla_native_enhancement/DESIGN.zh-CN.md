# 设计文档：InterFuser/Leaderboard �?CARLA 原生传感器增�?
## 1. 背景

当前仓库包含两条稳定的评测路径：
- `sensor_data_processor_module/`：对传感器数据应用后处理（SwinIR/SRGAN�?- `augmentation_seq_method/`：应用有序的后处理操作序�?
本新方法探索了一种不同的途径�?*�?CARLA 仿真层面升级传感�?*，而不是对输出进行后处理。这模拟了现实世界中在车辆上安装更好的相�?传感器的硬件升级场景�?
## 2. 目标

- **G1（硬件仿真）**：通过修改 CARLA 传感器配置来模拟现实世界的传感器升级（更高帧率、更高分辨率、更低噪声）�?- **G2（无需重新训练模型�?*：在不重新训练的情况下评估传感器升级下的模型性能，模�?即插即用"的硬件升级�?- **G3（Agent 适配�?*：调�?agent 处理频率以匹配传感器升级（例如，40Hz 相机对应 40Hz agent）�?- **G4（隔离）**：不破坏现有 baseline 脚本/agent�?- **G5（可比性）**：能够与后处理方法（`augmentation_seq_method`）直接对比�?
## 3. 非目�?
- 为新传感器配置重新训练或微调模型�?- 实现后处理操作（使用 `augmentation_seq_method` 来实现）�?- 改变模型架构或输入维度（除了分辨率变化所必需的）�?
## 4. 术语

- **高帧率（high_fps�?*：将相机帧率�?20Hz 提升�?40Hz，并�?40Hz 运行仿真�?- **高分辨率（high_res�?*：将相机分辨率从 800x600 提升�?1600x1200�?- **无噪声（no_noise�?*：禁�?CARLA 后处理效果（运动模糊、镜头光晕、泛光等）以模拟更清晰的传感器�?- **高斯噪声（gauss8/gauss16�?*：在 agent 端对 RGB 图像注入独立的高斯噪声（σ=8 �?16）。该设置不能与其�?token 组合�?- **原生增强（Native Enhancement�?*：在 CARLA 仿真层面应用的传感器改进，而非通过后处理�?
## 5. 高层架构

新方法位于：
- `./carla_native_enhancement/`

关键组件�?- **配置解析�?*：将 `NATIVE_ENHANCE` 解析为传感器配置参数�?- **传感器配置构建器**：根据解析的配置构建 CARLA 传感器规格�?- **Agent 集成**：修改后�?agent，能够适应不同的传感器配置�?- **传感器接口补�?*：修改后�?`SensorInterface`，正确处理高频数据�?
## 6. 配置接口

### 6.1 核心变量

- `NATIVE_ENHANCE`（string）：逗号分隔的增�?token�?  - 允许�?token：`none`、`high_fps`、`high_res`、`no_noise`、`gauss8`、`gauss16`�?  - 示例�?    - `none`（baseline�?0Hz�?00x600，默认噪声）
    - `high_fps`�?0Hz�?00x600，默认噪声）
    - `high_res`�?0Hz�?600x1200，默认噪声）
    - `no_noise`�?0Hz�?00x600，无后处理）
    - `high_fps,high_res`�?0Hz�?600x1200，默认噪声）
    - `high_fps,high_res,no_noise`�?0Hz�?600x1200，无后处理）
    - `gauss8`�?0Hz�?00x600，注入高斯噪�?σ=8；独立设置）
    - `gauss16`�?0Hz�?00x600，注入高斯噪�?σ=16；独立设置）

**约束**�?- `gauss8` �?`gauss16` 为独�?token，不允许与任何其�?token 组合�?
### 6.2 传感器配置映�?
| Token | 帧率 | 分辨�?| 运动模糊 | 镜头光晕 | 泛光 |
|-------|-----|--------|---------|---------|------|
| `none` | 20Hz (0.05s) | 800x600 | 0.45 | 0.1 | 0.675 |
| `high_fps` | 40Hz (0.025s) | 800x600 | 0.45 | 0.1 | 0.675 |
| `high_res` | 20Hz (0.05s) | 1600x1200 | 0.45 | 0.1 | 0.675 |
| `no_noise` | 20Hz (0.05s) | 800x600 | 0.0 | 0.0 | 0.0 |

### 6.3 世界仿真频率

**关键决策**：当启用 `high_fps` 时，世界仿真频率也必须提升到 40Hz�?
```python
# Baseline (20Hz)
settings.fixed_delta_seconds = 0.05  # 20Hz 世界 tick

# High FPS (40Hz)
settings.fixed_delta_seconds = 0.025  # 40Hz 世界 tick
```

**理由**�?- 确保传感器数据与世界状态同步�?- Agent 在每�?tick 都收到新鲜数据（无队列积压）�?- 模拟现实场景，其中控制循环频率与传感器频率匹配�?
### 6.4 Agent 处理频率

Agent �?`run_step()` 每个世界 tick 调用一次：
- Baseline：每�?20 次（�?0.05s�?- High FPS：每�?40 次（�?0.025s�?
**Agent 逻辑无需代码更改** - agent 自动适应世界 tick 速率�?
### 6.5 图像预处�?
对于 `high_res`�?600x1200）：
- 图像仍然�?resize �?224x224（或侧面相机�?128x128）后再输入模型�?- 模型输入维度保持不变�?- 好处：从更高分辨率下采样保留更多细节（抗锯齿效果）�?
### 6.6 输出/元信�?
每次 run 应记录：
- `native_enhance` 原始字符串�?- 解析后的配置（fps、分辨率、噪声设置）�?- 世界 tick 速率（`fixed_delta_seconds`）�?- 实际达到的帧率�?- �?tick 推理时间（验�?40Hz 是否可达）�?
### 6.7 评测 runner 的稳健性选项

继承 `augmentation_seq_method` 的所有稳健性选项�?- `PORT`、`TM_PORT`：端口配置，支持自动选择�?- `AUTO_START_CARLA`：自动启�?CARLA 服务器�?- `KILL_EXISTING_CARLA`：清理现�?CARLA 进程�?- `SENSOR_QUEUE_TIMEOUT`：传感器数据超时�?0Hz 时可能需要调整）�?- `CARLA_STARTUP_WAIT_SEC`、`CARLA_READY_TIMEOUT`：启动时序�?- `CLEANUP_KILL_PYTHON_ON_EXIT`：退出时进程清理�?
## 7. 技术挑战与解决方案

### 7.1 挑战�?0Hz 时传感器数据队列积压

**问题**：如果相机以 40Hz 运行�?agent �?20Hz 处理，传感器数据会在队列中累积�?
**解决方案**�?- 启用 `high_fps` 时以 40Hz 运行世界�?- Agent 自动�?40Hz 处理（每个世�?tick 调用一次）�?- 不会发生队列积压�?
### 7.2 挑战�?0Hz 时模型推理速度

**问题**：Agent 必须�?25ms�?/40s）内完成推理以保持实时性能�?
**分析**�?```
40Hz 预算：每�?25ms
典型 InterfuserAgent 推理：RTX 3090 �?15-20ms
余量�?-10ms（足够）
```

**缓解措施**�?- 监控�?tick 推理时间�?- 如果推理超过 25ms，记录警告但继续（仿真会减速）�?- 在元数据中记录实际达到的帧率�?
### 7.3 挑战：高分辨率计算成�?
**问题**：渲�?1600x1200 图像�?800x600 更昂贵�?
**影响**�?- CARLA 渲染�?30-50% GPU 时间�?- Agent 预处理（resize）：+10-20% CPU 时间�?- 总计：对于现�?GPU 仍在实时预算内�?
**缓解措施**�?- 监控 CARLA FPS 以确保维持目标速率�?- 如果 CARLA 无法�?high_res 维持 40Hz，考虑降低其他视觉设置�?
## 8. 实验矩阵

### 8.1 Baseline
- `none`�?0Hz�?00x600，默认噪声）

### 8.2 单一增强
- `high_fps`�?0Hz�?00x600，默认噪声）
- `high_res`�?0Hz�?600x1200，默认噪声）
- `no_noise`�?0Hz�?00x600，无后处理）
- `gauss8`�?0Hz�?00x600，高斯噪�?σ=8�?- `gauss16`�?0Hz�?00x600，高斯噪�?σ=16�?
### 8.3 两两组合
- `high_fps,high_res`�?0Hz�?600x1200，默认噪声）
- `high_fps,no_noise`�?0Hz�?00x600，无后处理）
- `high_res,no_noise`�?0Hz�?600x1200，无后处理）

### 8.4 三者组�?- `high_fps,high_res,no_noise`�?0Hz�?600x1200，无后处理）

**总计�? 种配�?*

## 9. 指标与报�?
主要（Leaderboard）：
- 路线完成度、驾驶得分、违规�?
次要（性能）：
- 实际达到的帧率（每秒世界 tick 数）�?- �?tick 推理时间（mean/std/p95）�?- CARLA 渲染 FPS�?- 每条路线 wall-clock 时间�?
对比�?- �?`augmentation_seq_method` 结果对比，评估硬�?vs 软件方法�?
## 10. 验证计划

- **V1**：`NATIVE_ENHANCE=none` 匹配原始 baseline 行为�?- **V2**：`high_fps` 达到 40Hz 且无超时或崩溃�?- **V3**：`high_res` 图像正确 resize 和处理�?- **V4**：`no_noise` 产生明显更清晰的图像�?- **V5**：全�?8 种配置端到端运行无错误�?- **V6**：每�?run 都生成元数据和日志�?
## 11. �?augmentation_seq_method 的对�?
| 方面 | augmentation_seq_method | carla_native_enhancement |
|------|------------------------|-------------------------|
| **方法** | 后处理（SwinIR/SRGAN/RIFE�?| 原生传感器升�?|
| **帧率** | 插值（20Hz �?40Hz 等效�?| 真实 40Hz 仿真 |
| **分辨�?* | 超分�?00x600 �?1600x1200�?| 原生高分辨率渲染 |
| **噪声** | 去噪（SwinIR�?| 从源头禁用噪�?|
| **计算** | Agent 端处�?| CARLA 端渲�?|
| **现实类比** | 软件升级 | 硬件升级 |

## 12. 风险与缓�?
- **风险�?0Hz 推理太慢**
  - 缓解：监控推理时间；如需要仿真会优雅减速�?  
- **风险：CARLA 无法维持 40Hz**
  - 缓解：降低视觉质量设置；监控实际 FPS�?  
- **风险：高分辨率导致内存问�?*
  - 缓解：监�?GPU 内存；如需要减�?batch size�?  
- **风险：模型在清晰图像上表现更�?*
  - 缓解：这是有效的实验发现；记录并分析�?
## 13. 预期结果

### 13.1 性能预测

| 配置 | 预期驾驶得分变化 | 理由 |
|------|----------------|------|
| `high_fps` | +5% �?+15% | 更快反应时间，更平滑控制 |
| `high_res` | +3% �?+10% | 更好的远距离物体检�?|
| `no_noise` | +10% �?+20% | 更清晰图像，更好的特征提�?|
| `high_fps,high_res,no_noise` | +15% �?+35% | 综合收益 |

### 13.2 与后处理的对�?
我们预期�?- 原生 `high_fps`�?0Hz�? 插�?FI�?0Hz �?40Hz�?- 原生 `high_res`�?600x1200）≈ 超分 SR�?00x600 �?1600x1200�?- 原生 `no_noise` �?去噪 DN（有噪声 �?清晰�?
这将验证硬件升级还是软件升级更有效�?

