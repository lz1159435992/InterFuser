# Provenance

Store reproducibility metadata per run and per host:

- commit hash
- branch
- dependency list
- hardware/runtime info
- run command
- config hash
- random seed

Suggested path pattern:

`provenance/<rq>/<run_id>/meta.json`
